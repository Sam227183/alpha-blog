<h1 class="text-center mt-4">Edit category name</h1>

<div class="container">
  <%= render 'form'%>
    <div class="text-center mt-2">
      <%= link_to '[ Cancel and return to category ]', category_path(@category), class: "text-secondary" %>
    </div>
  </div>
</div>